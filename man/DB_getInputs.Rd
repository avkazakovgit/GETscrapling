% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DB_getInputs.R
\name{DB_getInputs}
\alias{DB_getInputs}
\title{Fetch and Claim a Batch of Pending URLs}
\usage{
DB_getInputs(db_con, get_N = 10, tab_queue = "tab_queue")
}
\arguments{
\item{db_con}{A valid \code{DBIConnection} (SQLite ≥ 3.35 for RETURNING support).}

\item{get_N}{Integer(1). Number of URLs to fetch and claim.}

\item{tab_queue}{Character(1). Name of the queue table (default: "tab_queue").}
}
\value{
A character vector of claimed URLs. If none are pending, returns
a zero-length character vector.
}
\description{
Atomically selects the next \code{n} pending URLs from the queue, marks them
as \code{in_progress}, and returns their values. Uses a single CTE + UPDATE +
RETURNING statement to avoid race conditions in parallel sessions.
}
\examples{
con <- DB_load("mydb.sqlite")
urls <- DB_getInputs(con, get_N = 20, tab_queue = "tab_queue")
}
