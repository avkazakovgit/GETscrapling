% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DBinit_initTables.R
\name{DBinit_initTables}
\alias{DBinit_initTables}
\title{Initialize Database Tables from Definitions}
\usage{
DBinit_initTables(con, tab_defs)
}
\arguments{
\item{con}{A valid \code{DBIConnection} to a SQLite (or other DBI‐compliant) database.}

\item{tab_defs}{Named list of table definitions. Example:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{list(
  users = list(
    id   = list(init = "INTEGER PRIMARY KEY"),
    name = list(init = "TEXT NOT NULL")
  ),
  posts = list(
    id       = list(init = "INTEGER PRIMARY KEY"),
    user_id  = list(init = "INTEGER NOT NULL"),
    content  = list(init = "TEXT")
  )
)
}\if{html}{\out{</div>}}}
}
\value{
Invisibly returns \code{NULL}. Tables are created if they did not already exist.
}
\description{
Create multiple tables in a SQLite database according to a nested list of
table definitions. Each top‐level element of \code{tab_defs} is the name of a
table; its value is a named list of column definitions, where each column
definition must contain an \code{init} element (an SQL type + constraints string).
}
