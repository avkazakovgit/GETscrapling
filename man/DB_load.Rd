% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DB_load.R
\name{DB_load}
\alias{DB_load}
\title{Open and Configure an Existing SQLite Database Connection}
\usage{
DB_load(
  db_path,
  pragmas = c(busy_timeout = 30000, journal_mode = "WAL", synchronous = "NORMAL",
    temp_store = "MEMORY", foreign_keys = "ON", page_size = 16384, cache_size = -2000)
)
}
\arguments{
\item{db_path}{Character scalar. Path to the existing SQLite database file.}

\item{pragmas}{Named character or numeric vector of PRAGMA settings to apply.
Defaults are tuned for web-scraping concurrency (WAL mode, normal sync,
memory temp storage, etc.).}
}
\value{
A \code{DBIConnection} object with the specified PRAGMAs applied. The
caller is responsible for closing the connection via \code{DBI::dbDisconnect()}.
}
\description{
Opens a connection to an existing SQLite file and applies a set of PRAGMA
settings optimized for concurrent reads/writes and performance.
}
\examples{

# Open and configure:
con <- DB_load("mydata.sqlite")
# ... use con ...
DBI::dbDisconnect(con)
}
