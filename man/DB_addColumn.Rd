% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DB_addColumn.R
\name{DB_addColumn}
\alias{DB_addColumn}
\title{Add a New Column to an Existing Table}
\usage{
DB_addColumn(
  db_con,
  table,
  column,
  type = "INTEGER",
  default = 0,
  not_null = TRUE
)
}
\arguments{
\item{db_con}{A valid \code{DBIConnection} to the target database.}

\item{table}{Character(1). Name of the existing table to alter.}

\item{column}{Character(1). Name of the new column to add.}

\item{type}{Character(1). SQL data type for the column (e.g., "INTEGER", "TEXT").}

\item{default}{Scalar. Default value for the new column (quoted or unquoted as
appropriate for the SQL type).}

\item{not_null}{Logical(1). If TRUE, adds a NOT NULL constraint; otherwise allows NULLs.}
}
\value{
Invisibly returns TRUE on success. Throws an error if the operation fails
or if the column already exists.
}
\description{
Alters a table by adding a new column with the specified type, default value,
and optional NOT NULL constraint.
}
\examples{
con <- DB_load("mydb.sqlite")
DB_addColumn(con, "tab_queue", "retry_count", type = "INTEGER", default = 0)
}
