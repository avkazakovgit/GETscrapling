% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DB_initIndex.R
\name{DB_initIndex}
\alias{DB_initIndex}
\title{Initialize Database Indexes based on Definitions}
\usage{
DB_initIndex(db_con, ind_defs)
}
\arguments{
\item{db_con}{A valid \code{DBIConnection} to a SQLite (or other DBI) database.}

\item{ind_defs}{Named list: names are table names, values are named lists of
index definitions. Each index definition maps an index name to a character
vector of column names. Example:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{list(
  tab_queue = list(
    idx_pending_order = c("crawl_pending", "orderid"),
    idx_url           = c("URL")
  ),
  tab_crawl = list(
    idx_url_success   = c("URL", "crawl_success")
  )
)
}\if{html}{\out{</div>}}}
}
\value{
Invisibly returns \code{NULL}. On error, no indexes are created.
}
\description{
Creates all specified indexes in the database, each inside a single transaction.
}
