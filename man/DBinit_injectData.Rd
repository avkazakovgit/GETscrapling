% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DBinit_injectData.R
\name{DBinit_injectData}
\alias{DBinit_injectData}
\title{Populate Tables with Initial Data}
\usage{
DBinit_injectData(con, init_data)
}
\arguments{
\item{con}{A valid \code{DBIConnection} (e.g. to a SQLite database).}

\item{init_data}{A named list where names are table names (strings) and
values are tibbles or data.frames containing rows to insert.  Other tables
are left untouched.  If \code{NULL} or empty, nothing happens.}
}
\value{
Invisibly returns \code{NULL}.  On error, no partial inserts occur (all
are wrapped in a single transaction).
}
\description{
Given a named list of tibbles, append each tibble to its corresponding
table in the database. Tables must already exist with compatible schemas.
}
\examples{
\dontrun{
con <- DBI::dbConnect(RSQLite::SQLite(), "mydb.sqlite")
init_data <- list(
  tab1 = tibble(x = 1:3, y = letters[1:3]),
  tab2 = tibble(a = c(TRUE, FALSE))
)
DBinit_injectData(con, init_data)
DBI::dbDisconnect(con)
}
}
